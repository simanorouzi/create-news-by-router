export type ErrorType = {
  title: string;
  message: string;
};

export type newsType = {
  id: string;
  title: string;
  summery: string;
  description: string;
  image: string;
  date: Date;
};
